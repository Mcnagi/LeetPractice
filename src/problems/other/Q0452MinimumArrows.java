package problems.other;

public class Q0452MinimumArrows {
  public int findMinArrowShots(int[][] points) {
    int result = 0;
    //        Arrays.sort(points);
    //        int left = points[0][0];
    //        int right = points[0][1];
    for (int i = 1; i < points.length; i++) {}

    return result;
  }
}
